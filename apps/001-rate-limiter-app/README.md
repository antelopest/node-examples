# 1. Rate Limiter

Реализуй middleware для Express (на TS), который ограничивает количество запросов с одного IP:

- [x] 100 запросов за 1 минуту на роут.
- [x] Используй in-memory хранилище (Map).
- [x] После превышения лимита необходимо 429 Too Many Requests с заголовком Retry-After.
- [ ] Написать тесты (jest).

Дополнительно:

- [x] Возвращать клиенту html с информацией о текущем лимите и времени сброса лимита (начала нового периода)